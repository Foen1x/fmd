//Parser object that contains all parser for all skins
//Parser = function Parser() {}; //No need to initialize

Parser["layout:tr:bootstrap-blue"] = function(conf, prop) {
	
	return '<tr>${children}</tr>';
};

